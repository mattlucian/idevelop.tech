# npm configuration for idevelop-tech monorepo

# Ensure consistent lockfile generation across environments
package-lock=true
prefer-dedupe=true

# Use npm workspaces (already in package.json, but explicit here)
workspaces=true

# Reduce lockfile churn by using consistent package resolution
prefer-offline=false
save-exact=false
